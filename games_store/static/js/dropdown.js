

$(document).ready(function() {
	let options = {
		"coverTrigger": false,
	};
		
	$('.dropdown-trigger').dropdown(options);
	// var instance = M.Dropdown.getInstance($('.dropdown-triger'))
	//instance.open();
});